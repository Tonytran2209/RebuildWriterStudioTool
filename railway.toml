[build]
builder = "nixpacks"

[deploy]
startCommand = "DIST_PATH=dist node backend/node_modules/.bin/tsx backend/index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
