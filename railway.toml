[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && node_modules/.bin/tsx index.ts"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
