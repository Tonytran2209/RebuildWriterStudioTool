# Build Vite frontend, then serve via Express backend
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install",
  "cd backend && npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "cd backend && node_modules/.bin/tsx index.ts"
