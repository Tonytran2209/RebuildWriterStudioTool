providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "npm install --include=optional",
  "cd backend && npm install"
]

[phases.build]
cmds = ["NODE_ENV=development npm run build"]

[start]
cmd = "bash start.sh"
